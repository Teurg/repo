#---------------------------------------------------------------------------
# Dockefile to build Docker Image of Python web server
#
# Made by Andrey Piskunov   18.07.2024
#---------------------------------------------------------------------------

# Используем официальное окружение Python как базовый образ
FROM python:3.9-slim

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем текущие файлы в контейнер в директорию /app
COPY pserver.py .

# Открываем порт 8080 для доступа извне контейнера
EXPOSE 8080

# Запускаем server.py при старте контейнера
CMD ["python", "pserver.py"]
